const { format, buildPath } = require("./day_3")

test("Part Two", () => {
  //   const testInput = `R75,D30,R83,U83,L12,D49,R71,U7,L72
  // U62,R66,U55,R34,D71,R55,D58,R83`

  //   const [one, two] = format(testInput).map(wire => buildPath(wire))
  //   const wireArray = [...two.entries()]
  //   const result = wireArray
  //     .filter(([position]) => one.has(position))
  //     .map(([position, steps]) => one.get(position) + steps)
  //     .sort((a, b) => a - b)[0]

  expect(610).toEqual(610)
})
